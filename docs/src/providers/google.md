# Google

Authenticate with the Google Cloud CLI before using this provider:

```bash
gcloud auth application-default login
```

See `crates/gml-cli/providers/google/README.md` in the repository for setup details.
