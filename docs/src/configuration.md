# Configuration

`gml` reads provider configuration from `~/.gml/config.toml`.

Provider-specific settings (API keys, regions, SSH key names, and so on) are documented in the [Providers](providers.md) chapter.
